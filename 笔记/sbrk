1. 进程中的 sz 字段
定义：
sz 是进程结构体 struct proc 中的一个字段，表示 用户地址空间的当前大小。具体来说，它记录了进程堆（heap）的结束地址，即用户程序通过 sbrk 动态分配内存的虚拟地址上限。

作用：
当进程通过 sbrk(n) 请求扩展堆内存时，sz 会增加 n 字节，表示堆空间的扩展。
内核通过 sz 确定用户地址空间的合法范围，防止用户程序访问未分配的内存区域。

示例：
初始时，进程的堆空间可能为 0x1000（起始地址）到 sz = 0x2000（结束地址）。
调用 sbrk(0x1000) 后，sz 变为 0x3000，堆空间扩展为 0x1000 到 0x3000。

2. 为什么要返回旧的 sz（即 addr）？

系统调用规范
sbrk 的语义：sbrk(n) 应返回 扩展前的堆顶地址（即旧的 sz）。
例如：若原堆顶地址为 0x2000，调用 sbrk(0x1000) 后，新堆顶为 0x3000，但返回值是 0x2000。用户程序可以通过多次 sbrk 调用的返回值，追踪堆空间的扩展历史。

用户程序的需求
内存分配标识：返回旧的 sz 使得用户程序能够明确知道新增内存的起始地址。例如：
void *start = sbrk(4096); // 分配 4KB，start 是新内存的起始地址

3. 修改后的代码

uint64 sys_sbrk(void) {
  int n;
  if (argint(0, &n) < 0) return -1;
  struct proc *p = myproc();
  uint64 old_sz = p->sz;
  p->sz += n; // 仅扩展 sz，不分配物理内存
  return old_sz;
}

问题分析
虚拟地址扩展，但物理内存未分配：
	sz 被增加，但 growproc() 不再分配物理内存或更新页表。
	用户程序访问新扩展的地址（如 [old_sz, new_sz)）时，页表项（PTE）无效（PTE_V = 0）。
	触发 页面错误（Page Fault），但内核未实现处理逻辑，导致进程终止。

示例崩溃场景
运行 echo hi：
	echo 程序调用 sbrk() 扩展堆内存。
	尝试访问新分配的地址（如写入 0x4008），但该地址未映射物理内存。
	CPU 触发存储页面错误（scause = 15，stval = 0x4008）。
	xv6 的陷阱处理程序无法处理此错误，打印错误信息并 panic。
